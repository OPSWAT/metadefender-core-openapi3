language: node_js
node_js: 
  - "stable"
cache:
  directories:
    - "node_modules"
deploy:
  provider: pages
  skip_cleanup: false
  github_token: $GITHUB_TOKEN
  keep_history: true
  script: npm run gh-deploy  
  on:
    branch: master
